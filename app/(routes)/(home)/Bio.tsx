export default function Bio() {
  return <p className=" pointer-events-auto">{text}</p>;
}

const text = `aite eboigbe. digital designer and fullstack developer creating experiences that emotionally raise the bar and reward a shift in perspective. currently interning at new world group. previously at paredol.`;
